# selectors.yml
# -------------
# LedgerLine QA is test-driven through generic and singular tests enforcing schema and business rules.
# This selector runs them in isolation, making QA easy to execute independently in CI/CD.

selectors:
  - name: qa_tests
    description: "Run only QA-tagged tests"
    definition: "tag:qa resource_type:test"
