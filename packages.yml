# packages.yml
# ---------------------------------------------------------------
# In Ledgerline we install dbt_utils.
# dbt_utils provides the macros we rely on for history:
# surrogate_key, hash collision checks, unique combinations, etc.
# ---------------------------------------------------------------

packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1